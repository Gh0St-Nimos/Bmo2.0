function startGame(game) {
    if (game === 'jogo-da-velha') {
        window.location.href = 'Velha.html'; // Redirecionar para o jogo da velha
    } else if (game === 'jogo-da-forca') {
        window.location.href = 'forca.html'; // Redirecionar para o jogo da forca
    } else if (game === 'mastermind') {
        window.location.href = 'Mastermind.html'; // Redirecionar para o Mastermind
    } else if (game === 'mastermind') {
        window.location.href = 'Brick out.html'; // Redirecionar para o Mastermind
    } else if (game === 'Pac-man') {
    window.location.href = 'Pac-man.html'; // Redirecionar para o Mastermind
    }
    
}
